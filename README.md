# Exercism C++ Track

This repository contains my solutions to selected exercises from the [Exercism C++ track](https://exercism.org/tracks/cpp).

Each exercise lives in its own self-contained directory (with its own `CMakeLists.txt`, sources and tests).  
I work on them locally in CLion and run the official Exercism test suites via CMake / `exercism test`.
